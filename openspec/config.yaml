# SDD Configuration — convocatorias
# Auto-generated by sdd-init on 2026-06-16

context: |
  Web scraping system tracking open calls (convocatorias) from 50+ sources.
  Python 3.12, Scrapling, Playwright/Chromium for dynamic sites.
  Generates interactive HTML reports (Jinja2 + Chart.js).
  Runs via GitHub Actions daily.

strict_tdd: false

testing:
  runner: null
  runner_command: null
  layers:
    unit: false
    integration: false
    e2e: false
  coverage: false
  coverage_command: null
  linter: null
  type_checker: null
  formatter: null

persistence:
  mode: hybrid
  engram: true
  openspec: true

phases:
  proposal:
    required: false
  spec:
    required: true
    inputs: [proposal]
  design:
    required: true
    inputs: [proposal]
  tasks:
    required: true
    inputs: [spec, design]
  apply:
    required: true
    inputs: [tasks, spec, design]
  verify:
    required: true
    inputs: [spec, tasks]
  archive:
    required: false
    inputs: [verify]
